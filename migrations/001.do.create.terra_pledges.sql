CREATE TABLE terra_pledges (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  name TEXT NOT NULL,
  location TEXT NOT NULL,
  days INTEGER NOT NULL,
  date_created TIMESTAMP DEFAULT now() NOT NULL,
  likes INTEGER DEFAULT 0
);
